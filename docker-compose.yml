# docker-compose.yml (Final, Simple Version)
services:
  nsdc-website:
    build: .
    container_name: nsdc-website
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - VIRTUAL_HOST=nsdc.mec.ac.in
      - VIRTUAL_PORT=8080
